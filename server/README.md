## Mynance Backend

### How to build
`npm run build`

### How to start
`npm run start`

### How to serve with auto-reload
`npm install nodemon -g` and
`npm install concurrently -g` and
`npm run serve`