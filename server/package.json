{
  "name": "mynance",
  "version": "1.0.0",
  "description": "Mynance is the implementation for the binance API, the main purpose here is the learning process.",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/app.js",
    "start:build": "tsc -w",
    "start:run": "nodemon build/app.js",
    "serve": "concurrently npm:start:*"
  },
  "author": "derickfelix",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.18",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5"
  }
}
